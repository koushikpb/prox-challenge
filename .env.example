# Server-only — read in src/app/api/chat/route.ts and scripts/*.ts.
# Never exposed to the browser bundle.
ANTHROPIC_API_KEY=your-api-key-here

# Client-side opt-in for the voice composer skeleton.
# Leave unset or "false" for the default text-only composer.
NEXT_PUBLIC_VOICE_ENABLED=false
